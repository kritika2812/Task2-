Now create one container using docker image named as newimage:v1  -> docker run -it --name c1 -p 8085:8080 -v /:/host newimage:v1
Now we have to setup jenkins in RHEL 8 

after setting up jenkins , we need to install four plugins :-
Github Plugin
SSH Plugin
Email Notification Plugin
SMTP Plugin




